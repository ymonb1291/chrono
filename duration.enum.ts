/** 
 * Duration of weeks, days, hours, minutes and seconds in milliseconds
 * @private
 * @enum
 */
export enum Duration {
  WEEK = 604800000,
  DAY = 86400000,
  HOUR = 3600000,
  MINUTE = 60000,
  SECOND = 1000,
}